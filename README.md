# Acompanhamento Diario

Dashboard comercial em Python preparado para hospedagem gratuita no Render.

## Hospedagem no Render

- Build Command: `pip install -r requirements.txt`
- Start Command: `python dashboard.py`

O app usa `data.json` com os dados exportados da planilha.
